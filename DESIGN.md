# QMAH-Docs 版面設計說明

## 快速理解

| 先問自己 | 文件直接回答 |
| --- | --- |
| Why（為什麼要看這頁） | 文件站的版面不是裝飾，它會決定讀者能不能從工作入口找到正規規則，也會影響寬表格、鍵盤操作和窄螢幕閱讀。改一個元件時要先知道它在查閱流程中的責任。 |
| What（現在實際怎麼排） | 首頁提供準備環境、查詢系統和交付規則三個入口，接著列出 Shared、Operations 與五個功能系統；詳細頁用固定的「快速理解」、查詢入口、流程、表格和檢查段落。視覺使用 QMAH field index，並保留 focus、表格水平捲動和 `prefers-reduced-motion` 行為。 |
| How（調整版面或內容怎麼走） | 先確認變更是首頁索引、快速頁資訊層級、詳細文件內容或共用 CSS，再依本頁的首頁結構、內容規則、響應式與可及性逐項檢查。新增文字要放在讀者需要的位置，新增樣式要有功能範圍，最後用桌面、窄螢幕、鍵盤和 reduced-motion 實際查看。 |

**適用情境：** 要調整文件首頁、快速查閱頁、表格、Diagram、共用 CSS 或窄螢幕版面時，先用本頁確認這項改動會不會破壞查找順序、資訊密度、鍵盤操作或文字替代途徑。

本文件記錄文件站的版面規則、內容入口與檢查結果，供後續調整時核對。

## 版面目標

首頁列出環境、資料、系統與交付文件的入口，並保留系統邊界、資料契約與來源脈絡。首頁是文件索引，不放產品宣傳內容；查閱時可先確認共同基準，再進入 Shared 或任一功能系統。

## 視覺規則

- 方向：QMAH field index／accession ledger，以文物典藏索引、登錄簿、頁邊標籤與技術欄位筆記作為視覺語彙。
- 色彩：墨藍文字、瓷青底色、青綠輔助色與少量硃砂標記；深色模式保留同一組關係。
- 排版：穩定的無襯線正文、等寬字體標示路徑與命令、細水平線表示閱讀分段。
- 元件：工作索引、七個入口列、文件目錄與表格；各文件維持清楚的連結層級，不使用卡片包裝每一份文件。
- 動效：只使用短暫的焦點／滑入提示；`prefers-reduced-motion` 時停用過渡。

版面概念採用「QMAH field index／accession ledger」；這項概念只約束文件站版面，不延伸成產品功能或品牌故事。

## 首頁結構

1. 開頭顯示「準備環境、查詢系統、查交付規則」三個入口。
2. 接著顯示七個快速查閱頁，固定順序為 Shared、Operations、Catalog、Game、Social、User、Store；側欄再分成「共用與營運」及「五個功能系統」兩組。
3. 頁面下方保留依文件目的分組的詳細文件目錄，供按任務查閱或直接定位。
4. 頂端與首頁同時保留 QMAH、QMAH-Docs、QMAH-Database 專案網址，以及 QMAH-Docs 文件站網址。

## 內容與版面規則

- 五個功能系統與 Shared 的快速查閱頁固定使用相同的標題、查詢入口欄位與入口列順序；Operations 以管理後台接手內容取代使用者前台內容；詳細規則回到單一 canonical 文件。
- 每個可閱讀 Markdown 頁面在標題後先放「快速理解」，依 Why（為什麼）、What（目前內容）、How（怎麼使用）三列回答；Why 要說明讀者會遇到的工作，What 要指出本頁涵蓋的實際元件、資料或規則，How 要給出查詢、修改或串接的起點與順序。三列不能只寫「本頁說明」或把讀者丟回目錄。
- 開發文件用中性說明，不預設閱讀者的身分、工具偏好或是否使用 AI。
- 命令、路徑、API、SQL 識別字與版本保留可複製形式；解釋文字採繁體中文。
- 表格用於資料表字典、欄位對照或系統映射；長篇流程使用編號步驟。資料表說明集中在「資料表參考」，Diagram 頁只負責 SSMS 圖表建立與閱讀；連線只表達方向，關係文字放在節點、圖例或旁註。
- 連結文字直接說明目的，不用「點這裡」這類無法從搜尋結果判斷內容的文字。

## 響應式與可及性

- 寬度縮小時，首頁工作索引與系統導覽改為單欄，七個快速入口仍保持可掃讀；資料表由外層捲動容器承接水平捲動，表頭與內容維持同一個原生 table layout，不讓整個頁面被寬表格撐開。
- 導覽、連結與自訂首頁列均保留鍵盤 focus 狀態。
- 顏色只作為輔助；系統名稱、代碼與文字標籤同時呈現。
- 程式碼區塊、表格、Diagram SVG 圖表與純文字說明互相補充，不讓單一視覺形式成為唯一資訊來源。
- 不使用漸層、玻璃擬態、巨大標語、厚重彩色側條或不必要的圓角卡片。

## 檢查紀錄

已完成 impeccable source-based review、官方參考核對、VitePress build、Markdown lint、內部連結檢查、機械檢查與桌面／窄螢幕瀏覽器檢查。

已修正失效錨點、來源連結副檔名、鍵盤 focus ring、語意導覽、layout transition、表格橫向瀏覽與窄螢幕資訊密度；本次再以 320px、390px、768px 與桌面寬度檢查長表格和首頁入口。

首頁保留七個快速查閱入口、文件目錄、四個固定外部入口、單欄響應式規則與 reduced-motion 規則。後續內容變更應遵守本文件的資訊層級與中性語氣。
