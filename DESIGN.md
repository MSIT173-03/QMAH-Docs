# QMAH-Docs 版面設計說明

本文件記錄文件站版面方向、內容入口與完成檢查，讓後續調整不必重新猜測這次的設計判斷。

## 設計命題

QMAH-Docs 要讓下一個開發動作可見，同時保留系統邊界、資料契約與來源脈絡。首頁是「工作索引」，不是產品宣傳頁；閱讀流程先確認共同路線，再進入五個功能系統或共用基礎頁。

## 視覺世界

- 方向：QMAH field index／accession ledger，以文物典藏索引、登錄簿、頁邊標籤與技術欄位筆記作為視覺語彙。
- 色彩：墨藍文字、瓷青底色、青綠輔助色與少量硃砂標記；深色模式保留同一組關係。
- 排版：穩定的無襯線正文、等寬字體標示路徑與命令、細水平線表示閱讀分段。
- 元件：工作索引、六系統列、完整目錄與表格；不把每份文件包成互相競爭的卡片。
- 動效：只使用短暫的焦點／滑入提示；`prefers-reduced-motion` 時停用過渡。

這次方向由 impeccable 的 concept seed 選定，seed `06ffa6b9` 對應「QMAH field index／accession ledger」。方向只約束文件站版面，不延伸成產品功能或品牌故事。

## 首頁結構

1. 開頭先顯示「建立基線、進入系統、查閱交付」三個工作索引。
2. 接著顯示六個快速查詢頁：Catalog、Game、Social、User、Store 與 Shared。
3. 頁面下方保留完整六組文件目錄，支援循序閱讀與直接定位。
4. 頂端與首頁同時保留 QMAH、QMAH-Docs、QMAH-Database 專案網址，以及 QMAH-Docs 文件站網址。

## 內容與版面規則

- 快速查詢頁只聚合入口、資料關係、API、畫面與檢查點；詳細規則回到單一 canonical 文件。
- 開發文件用中性說明，不預設閱讀者的身分、工具偏好或是否使用 AI。
- 命令、路徑、API、SQL 識別字與版本保留可複製形式；解釋文字採繁體中文。
- 表格只在欄位對照或系統映射確實比段落清楚時使用；長篇流程改用編號步驟。
- 連結文字直接說明目的，不用「點這裡」這類無法從搜尋結果判斷內容的文字。

## 響應式與可及性

- 寬度縮小時，首頁工作索引與系統導覽改為單欄，六個快速入口仍保持可掃讀。
- 導覽、連結與自訂首頁列均保留鍵盤 focus 狀態。
- 顏色只作為輔助；系統名稱、代碼與文字標籤同時呈現。
- 程式碼區塊、表格、Mermaid 圖與純文字說明互相補充，不讓單一視覺形式成為唯一資訊來源。
- 不使用漸層、玻璃擬態、巨大標語、厚重彩色側條或不必要的圓角卡片。

## Review verdict

本版已完成兩份 impeccable source-based review、官方參考核對、VitePress build、Markdown lint、內部連結檢查與機械檢查；未進行實際瀏覽器畫面檢視。依審查結果修正失效錨點、來源連結副檔名、鍵盤 focus ring、語意導覽、layout transition、表格橫向瀏覽與窄螢幕資訊密度。結論是：首頁工作索引、六系統快速入口、完整目錄、四個固定外部入口、單欄響應式規則與 reduced-motion 規則均保留；後續內容變更仍須遵守本文件的資訊層級與中性語氣。
